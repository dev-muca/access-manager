import Alert from "@/components/Alert";
import Container from "@/components/Container";

const Private = () => {
  return (
    <Container title="ÁREA PRIVADA">
      <Alert title="Você está em uma área privada!" hasActions />
    </Container>
  );
};

export default Private;
