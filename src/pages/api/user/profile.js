import Profile from "@/services/database/profile-methods";

export default async function handler(req, res) {
  // try {
  //   const { id, role, avatar } = req.body;
  //   const update = await Profile.updateProfileInfoByUserId(id, { role, avatar });
  //   if (!update) {
  //     return res.status(500).send({ message: "Erro ao salvar dados do perfil" });
  //   }
  //   res.status(200).send({ message: "Alterações do perfil salvas" });
  // } catch (err) {
  //   console.log(err);
  //   res.status(400).send({ message: "Erro ao salvar dados do perfil" });
  // }
}
